import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#121212', // Replace with your desired primary color
    },
    secondary: {
      main: '#FFFFFF', // Replace with your desired secondary color
    },
  },
});

export default theme;